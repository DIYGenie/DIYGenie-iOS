import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Your profile will appear here.")
                .font(.title3)
        }
        .padding()
    }
}
