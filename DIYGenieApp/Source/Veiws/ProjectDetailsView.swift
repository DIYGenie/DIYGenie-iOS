import SwiftUI

struct ProjectDetailsView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Project details will appear here.")
                .font(.title3)
        }
        .padding()
    }
}
